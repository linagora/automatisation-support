import type { GlobalPipelineGroup } from "../typesTextAnalysisDataset";

export const multiTopicGroups: GlobalPipelineGroup[] = [
  { id: "multi-topic", label: "Cases involving more than one support topic", tags: ["multi-topic"] }
];
